interface Task {    //I tried to create a folder for interfaces but could not get that to work so I am putting the interface here in Components
    id: number;
    title: string;
    body: string;
}

export default Task;